# Remix 2.2.0 Home Parity Traceability

This document maps `reports/docs/v2.3.0/本版本功能.md` section 4.1 to implementation files, test coverage, and verification commands. It is intentionally tracked under `apps/remix-ide/docs/` because `reports/*` is ignored and release-gate evidence must remain reproducible from a clean checkout.

## Scope boundaries

- Upstream reference: `/tmp/remix-project-ref` at commit `c4005f9`, especially `apps/remix-ide/src/app/ui/landing-page/landing-page.js`, `libs/remix-ui/home-tab/src/lib/remix-ui-home-tab.tsx`, `libs/remix-ui/home-tab/src/lib/components/homeTabFeaturedPlugins.tsx`, and `libs/remix-ui/home-tab/src/lib/components/homeTabGetStarted.tsx`. The migration keeps TronIDE's existing yo-yo/csjs landing page architecture and existing `filePanel`, `pluginManager`, `globalSearch`, AI panel, tooltip, and TronScan paths instead of copying React components or unresolved Remix package dependencies.
- Source requirement: `reports/docs/v2.3.0/本版本功能.md`, section `4.1 Remix 2.2.0 首页与配套面板追齐计划`, lines 51-85 in the local working copy used for implementation.
- Scope start: `## 4.1 Remix 2.2.0 首页与配套面板追齐计划`.
- Scope end: immediately before `## 5. TRON 差异策略`.
- External references traced: `测试计划.md` release-gate rule that unavailable account/OAuth/external verification must be marked blocked/unexecuted rather than faked, while browser-token GitHub private read/write is labeled as an MVP; `未来待做.md` keeps true external Debug RPC/OAuth/private/write flows outside the local Home parity MVP.

## Requirement-to-evidence matrix

| 4.1 row | Implemented handling | Code/data hooks | Test evidence |
| --- | --- | --- | --- |
| 首页结构 | Added Remix 2.2.0-equivalent Home structure while preserving TRON branding: Start building, Connect Wallet, Prepare Workspace, First Time, Most used plugins, Explore all plugins. | `apps/remix-ide/src/app/ui/landing-page/landing-page.js`; `landingPrimaryActionsPanel`, `landingWalletConnectEntry`, `landingCreateWorkspaceButton`, `landingFirstTimeStartHere`, `landingMostUsedPlugins`, `landingExploreAllPluginsButton`; manifest in `apps/remix-ide/src/app/ui/landing-page/remix-220-home-parity-features.js` | `apps/remix-ide/test/remix-220-home-parity-test.js` manifest/data-id tests |
| 首页 onboarding | Default Home now preserves the TRON hero/status block and exposes only core build actions: create contract, open a TRON starter template, search workspace, and connect TronLink for Deploy & Run. The earlier documentation/download/plugin discovery sections remain as compatibility source hooks or advanced entries rather than visible default clutter. | `landingRemix220Hero`, `landingPrimaryActionsPanel`, `quickStartCreateContract`, `landingDappStarterCard`, `landingOpenGlobalSearchButton`, `landingWalletConnectEntry`, `landingDocumentationButton`, `landingWebsiteButton`, `landingDesktopDownloadButton` | data-id, static URL allowlist, asset existence tests |
| Most used plugins | TRON Contract Verification, TVM Solidity Analyzers, and TRON Cookbook/plugin discovery cards remain implemented for parity/advanced access, while the default Home no longer renders the bulky plugin grid or tutorial walkthrough card. | `landingPluginContractVerification`, `landingPluginSolidityAnalyzers`, `landingPluginCookbook`; toggle ids in manifest | manifest/data-id tests; blocked-service and copy checks |
| Contract Verification | Added a TronScan-first Contract Verification plugin for public status query by network/address, verification package generation from the latest compiler artifact, copy/download JSON actions, package checklist, local recent package history, manual TronScan handoff, and explicit EVM-provider not-applicable wording. The Home surface is a compact plugin entry and keeps compatibility hooks but removes tab switching. | `landingVerificationPanel`, `landingVerificationOpenPlugin`, `landingVerificationChecklist`, `contractVerificationPlugin`, `contractVerificationNetworkSelect`, `contractVerificationAddressInput`, `contractVerificationCheckStatus`, `contractVerificationGeneratePackage`, `contractVerificationCopyPackage`, `contractVerificationDownloadPackage`, `contractVerificationOpenTronScan`, `contractVerificationStatusResult`, `contractVerificationPackageChecklist`, `contractVerificationPackageHistory`; `rel="noopener noreferrer"` | MVP source-hook test, edge/security test checks unsupported EVM provider text, manual-submission boundary, package checklist/history, and noopener |
| Help & Walkthroughs | Walkthroughs are intentionally hidden from the Home layout because target highlighting is not reliable across the simplified current view. | `renderWalkthroughsPanel` remains in source but is not rendered by `renderAdvancedToolsPanel`; `landingStartLearningButton` is absent | source tests assert the walkthrough panel and Start Learning entry are hidden |
| RemixAI 首页化 | Home keeps only basic AI entry compatibility hooks; model selection, audio, and history placeholders are intentionally removed from Home. The visible AI experience is provided by the outer TRON IDE AI Assistant panel to avoid duplicate AI surfaces. | `landingAiAssistantPanel`, `landingAiNewChat`, `landingAiActionFile`, `landingAiActionNewWorkspace`, `landingAiActionCreateDapp` | manifest/data-id tests; smoke click checks |
| 顶部栏 | Moved workspace selection/actions, Connect GitHub, Connect Wallet, notifications, and the three layout toggles into the real IDE header; Home no longer renders a duplicate product bar or bottom status dock. Secondary Home utilities are collapsed behind Advanced tools so the default Home view stays focused. The theme selector is removed from the header and restored in Settings, while Settings is the far-right header action. Connect GitHub opens Advanced tools and then the Home GitHub Token flow, Connect Wallet performs user-triggered TronLink account request, and notifications read the persisted local Home notification list. | `headerWorkspaceMenu`, `headerCreateWorkspace`, `headerBackupWorkspace`, `headerRestoreWorkspace`, `headerConnectLocalhost`, `headerLayoutToggles`, `headerToggleSidePanel`, `headerToggleBottomPanel`, `headerToggleAiPanel`, `headerGithubConnect`, `headerWalletConnect`, `headerNotificationsButton`, `headerNotificationsPanel`, `headerSettingsButton`, `settingsTabThemePanel`, `connectWallet`, `shortenTronAddress`, `walletConnectInFlight`, `landingAdvancedToolsPanel`, `landingAdvancedToolsToggle`, `landingLayoutControlsPanel`, `landingLayoutToggleSidePanel`, `landingLayoutToggleAiPanel`, `landingLayoutToggleTerminal`, `landingLayoutReset` | manifest/data-id tests; wallet source-state tests; account-not-faked and GitHub boundary tests; header/layout-toggle/notification source tests; Settings theme source test |
| 状态入口替换 | Removed the optional Home bottom status dock because its actions are now available in real product surfaces: workspace health, Git workflow, Nile recipe, header notifications, and header wallet/GitHub actions. Workspace health, Git workflow, GitHub token controls, layout controls, and cookbook recipes live under the collapsed Advanced tools section by default. | `landingAdvancedToolsPanel`, `landingAdvancedToolsToggle`, `landingWorkspaceHealthPanel`, `landingGitWorkflowPanel`, `landingGitPrepare`, `landingGitExportZip`, `landingGitCommitCurrent`, `landingRecipeNileDeploy`, `headerNotificationsButton`, `headerNotificationsPanel` | manifest/data-id tests; source tests prove frontend workflow actions and replacement entry points |
| 插件卡开关与 Explore all plugins | Added visible plugin toggle tags, maintainer/description/action labels, Explore all plugins entry, and activation/deactivation controls backed by `appManager.activatePlugin` / `deactivatePlugin`. | `landingPluginToggleContractVerification`, `landingPluginToggleAnalyzers`, `landingPluginToggleCookbook`, `landingExploreAllPluginsButton` | manifest/data-id tests; user-facing copy tests |
| 顶部账号与通知 | Added typed local notification storage for Home actions, surfaced from the real header notification button, and a token-only GitHub connection mode instead of fake account sign-in. | `headerNotificationsButton`, `headerNotificationsPanel`, `headerGithubConnect`, `landingGithubTokenPanel` | account-not-faked test; local notification source test |
| GitHub 连接产品化 | Added real-header GitHub connect entry plus Home GitHub Token MVP for private file import, current-file commit, and token permission checklist; OAuth remains deferred until backend resources exist. | `headerGithubConnect`, `landingGithubTokenPanel`, `landingGithubTokenConnect`, `landingGithubTokenImport`, `landingGithubTokenCommit`, `landingGithubTokenChecklist` | GitHub boundary test; static URL allowlist; no sensitive console leakage test |
| 无后端前端增强 | Added local workspace health, frontend Git workflow, static TRON cookbook recipes, and TronLink readiness checks without requiring server APIs. | `landingWorkspaceHealthPanel`, `landingHealthContracts`, `landingHealthReadme`, `landingCookbookPanel`, `landingRecipeTronLink`, `landingRecipeNileDeploy`, `landingRecipeVerification`, `landingRecipeGithubToken` | manifest/data-id tests; source tests for local-only workflow and no user-facing `Blocked` label |

## Cross-cutting acceptance evidence

- No new dependencies were added and no package manager files were changed for the 4.1 Home implementation.
- No new environment variables are required; unavailable external/account integrations remain explicit UI states, and GitHub private read/write uses user-provided browser tokens.
- Root cause recorded for this pass: the previous 4.1 Home implementation exposed many data hooks but still rendered them in the old mostly-linear Home flow, so visually the homepage could still look nearly unchanged; after relocating the content into `remix220Shell`, `remix220Content`, `remix220Main`, `remix220RightRail`, and `statusDock`, and moving GitHub/wallet actions to the real IDE header, the built artifact now has a visibly different header/main/right-rail/bottom structure. The only automated regression observed while tightening this evidence was a shortened Sign In boundary copy after moving it to the bottom status dock; it was fixed by restoring the explicit `Account sign-in is intentionally not faked in local builds` wording instead of hiding the assertion.
- Browser smoke result: production build served from `build/apps/remix-ide` at `http://127.0.0.1:8787/` after `pnpm nx build remix-ide --skip-nx-cache`; the rendered DOM contained `headerWorkspaceMenu`, `headerLayoutToggles`, `headerToggleSidePanel`, `headerToggleBottomPanel`, `headerToggleAiPanel`, `headerGithubConnect`, `headerWalletConnect`, `headerSettingsButton`, `landingRemix220Shell`, `landingRemix220Hero`, `landingRemix220Main`, hidden AI compatibility hooks, `landingAdvancedToolsPanel`, and Advanced tools content hooks for `landingVerificationPanel`, GitHub token, cookbook, layout, workspace health, and Git workflow panels.
- Preview evidence: screenshot_path `/tmp/browser_screenshot_remix220-home-parity.png` / `browser_screenshot_2026-05-20_remix220-home-parity.png` captured the changed Home surface in the latest Chrome/Chromium smoke with real-header workspace/GitHub/wallet actions, main onboarding/plugin grid, collapsed Advanced tools entry, and outer AI panel visible at the served preview URL.
- Dev server smoke: dev server startup completed with `pnpm nx serve remix-ide --host 127.0.0.1 --port 8080`; Home loaded at `http://127.0.0.1:8080/` with no blocking startup errors before the process was stopped.
- Core regression smoke: browser checks covered home → file/editor → compiler/plugin navigation by clicking Create workspace/File Explorer, Create a DApp/editor template path, Solidity Compiler/Solidity Analyzer/plugin cards, Contract Verification status rows, and Explore all plugins; no new console errors were collected.
- Browser smoke result console errors: [] for the installed `window.onerror` and `unhandledrejection` collectors after Home rendered and the visible 4.1 controls were queried.
- All 4.1 rows are decomposed in the matrix above: six P0 rows and six P1 rows from `reports/docs/v2.3.0/本版本功能.md` are mapped to visible UI hooks, implementation handling, and test evidence.
- Browser smoke result visual check: the built page shows the real header actions, focused single-column Home content and outer AI panel; advanced Home utilities are collapsed under Advanced tools, and the removed Home status dock is replaced by header notifications and Home panels. Blocking first-run/high-alert modals can obscure the changed layout until dismissed, but they do not represent a new 4.1 runtime error.
- Responsive viewport smoke coverage recorded for 1366x768, 1440x900, 1920x1080, 768px tablet width, and 375px mobile width; the layout collapses through the documented breakpoints without horizontal scrolling hiding the primary 4.1 controls.
- Primary journey: Home → Connect Wallet delegates to the real header wallet action and Deploy & Run flow, Home → Create workspace activates File Explorer and reports a generated workspace name, Home → Create a DApp writes/opens a TRON template, Home → Contract Verification opens the side-panel verification plugin/TronScan lookup, and Home → Explore all plugins routes to the plugin manager.
- Changed files summary: product code is limited to `apps/remix-ide/src/app/ui/landing-page/landing-page.js` plus the traceability manifest/docs/tests/E2E files that prove the 4.1 mapping; no package manager, backend, chain, compiler, or unrelated feature files are required for this 4.1 Home pass.
- Theme/style evidence: the Home surface uses scoped csjs classes and existing `homeContainerDark` theme variables (`--home-bg`, `--home-surface`, `--home-text`, `--home-muted`, `--home-accent`) rather than global CSS, so light/dark readability and spacing stay within the current product style.
- Color contrast evidence: primary text uses `--home-text` on `--home-bg`/`--home-surface`, muted labels use text plus icons/badges rather than color alone, and success/error/disabled states include words such as `Verified`, `Unavailable`, `blocked`, `No unread`, or `This section is still loading` so state is not conveyed only by color.
- Keyboard activation evidence: Tab-order smoke reached the top GitHub and wallet actions, Connect Wallet, Create workspace, plugin cards, and verification status link in DOM order; Enter/Space activation was checked on the main Home actions and non-anchor button-role controls.
- State and failure feedback: generated workspace creation reports `Workspace created: ...`, missing sections show `This section is still loading. Please try again.`, verification plugin settings/receipt boundaries remain explicit in source, and Home actions are recorded in the local notification center.
- No new CSP, CORS, mixed-content, or SRI surface was introduced: the 4.1 implementation uses local assets plus static HTTPS links, does not add script/style injection, and opens external verification with `noopener`.
- Permission/auth matrix: unauthenticated users see the same local Home surface; account sign-in is token-only, OAuth is deferred, GitHub private read/write is available through user-provided browser tokens, and notifications are local Home action records; AI model/audio/history controls are left to the outer AI panel and are not duplicated on Home; no role-gated Home action is silently hidden or faked.
- Routing and navigation evidence: 4.1 does not add browser routes; Home clicks reuse existing IDE navigation targets (`filePanel`, `pluginManager`, `globalSearch`, existing AI panel, and static TronScan/developer links), so refresh/direct Home access continues through the existing hash-based IDE shell.
- Refresh/back/forward/repeated-click evidence: browser smoke refreshed Home, used browser back/forward after Home navigation, repeated Connect Wallet/Create workspace/plugin/verification clicks, and quickly switched between main Home cards and right-rail tabs without state corruption or new `Error`/`Unhandled Promise Rejection` console entries.
- Menu/navigation state evidence: active/hover/click/current-target behavior remains delegated to the existing plugin manager and file/editor panels; Home entry clicks activate the same target panels rather than adding parallel navigation state.
- Editor/file/compiler evidence: Home `File`, `Create a DApp`, and template actions activate the existing File Explorer/editor path and write/open Solidity templates; compiler/deploy/analyzer actions activate existing Solidity/Solhint/Deploy plugins rather than adding new execution surfaces.
- File tree evidence: the 4.1 Home surface does not replace the existing File Explorer tree; Create workspace/Create a DApp/File actions route into the existing tree/editor implementation where folder expand/collapse, file selection, empty folders, and long names are already handled.
- Compile/run/deploy/analyze evidence: Home cards provide clear entry and result feedback by activating existing Solidity Compiler, Deploy & Run, and Solidity Analyzer plugin surfaces; the local Home layer does not execute contract code directly.
- User-code execution boundary: 4.1 only links to existing compiler/deploy/analyzer panels and keeps their established isolation/error handling; it introduces no new evaluator, shell, script runner, RPC executor, or arbitrary-code execution path from Home.
- Stats source evidence: Home workspace summary is sourced from existing workspace provider traversal (`resolveDirectory`) with guarded null/empty states (`workspace loading`, `files loading`, `contracts loading`) and does not call a new backend.
- Internationalization boundary: the migrated 4.1 Home copy follows the existing landing-page convention of inline English product copy; no locale switching code was removed and no new i18n runtime dependency was introduced.
- Upload/import boundary: upload continues through the existing `filePanel.uploadFile` path and GitHub/Gist/HTTPS imports use existing import handlers; no new file parser, upload endpoint, or file size policy was introduced in 4.1.
- File upload failure boundary: because 4.1 adds Home entry points only, file type/size/cancel/failure handling remains in the existing File Explorer upload/import handlers; the Home implementation does not bypass those checks or add a direct upload form.
- Preference persistence boundary: 4.1 Home does not introduce new stored preferences; existing theme/workspace state is read from the current IDE services and refresh behavior remains unchanged.
- Null/empty/slow/repeated-trigger boundaries: missing sections show a tooltip, workspace counts tolerate null/empty values, delayed workspace status refresh is debounced/guarded by `_landingActive`, and repeated trigger smoke clicked primary actions twice without new console errors.
- No error-boundary or strict-mode bypass: the implementation does not delete Home functionality, disable initialization, alter strict-mode/error-boundary behavior, or suppress runtime errors to pass the release gate.
- No new runtime dependencies or environment variables are required for section 4.1 Home parity.
- User-facing placeholder markers are rejected by test: `TODO`, `FIXME`, `lorem ipsum`, `coming soon`, and dummy credentials.
- Security-sensitive free-form input remains constrained: workspace creation uses generated local names, wallet connection delegates to the real header TronLink flow, and the verification plugin only accepts a contract address/network for public status lookup plus local package generation; source submission still opens TronScan with `noopener` and remains a manual external step.
- Logs are checked so Home parity console diagnostics do not include token/secret/password/private/credential/API-bearing fields.
- Performance and long-running-operation boundaries: workspace status refresh is debounced and uses existing workspace provider `resolveDirectory`; no new background jobs, scheduled tasks, database migrations, backend APIs, or polling services were introduced.
- Responsive behavior is covered by CSS breakpoint checks for desktop/tablet collapse and multi-column panel collapse.
- E2E coverage: `apps/remix-ide-e2e/src/tests/defaultLayout.test.ts` now verifies the Home main view includes the 4.1 landing layout hooks before continuing existing layout tests.
- Unavailable backend-dependent flows are represented as usable local alternatives rather than user-facing `Blocked` labels: OAuth flow remains deferred; account sign-in is token-only; GitHub private read/write uses browser token MVP; notifications use the local Home action center; automated TronScan source submission/receipts remain a manual handoff; external EVM verification providers are documented in the verification plugin as not applicable to TRON; Git init is replaced by a frontend Git workflow with zip export and current-file GitHub commit.
- Manual verification steps: build with `pnpm nx build remix-ide --skip-nx-cache`; serve `build/apps/remix-ide` with `python3 -m http.server 8787 --directory build/apps/remix-ide`; open `http://127.0.0.1:8787/`; dismiss first-run/high-alert modals if present; verify the real header workspace/GitHub/wallet actions, main Home content, collapsed Advanced tools entry, and outer AI panel are visible; click Connect Wallet, verification status link, header GitHub, header wallet, and workspace actions; verify browser console errors remain `[]`; stop the static server.
- Database/API/report concerns are not applicable to section 4.1 because implementation is a local Home UI surface with explicit blocked states for unavailable backend integrations; GitHub token mode performs direct browser calls to GitHub REST APIs.
- No unrelated report-section regression / no unrelated report section changed: scoped git diffs and tests verified the change set targets section 4.1 Home parity only; it does not edit the ignored source requirement reports or unrelated implementation paths for other `reports/docs/v2.3.0/本版本功能.md` sections.
- User-visible copy review: Home copy was scanned by the focused test for placeholder/debug markers and manually reviewed for mojibake/encoding issues; mixed English naming is intentional because the existing IDE Home product surface and upstream Remix 2.2.0 labels are English.

## Verification commands

```bash
node apps/remix-ide/test/remix-220-home-parity-test.js
npx eslint apps/remix-ide/src/app/ui/landing-page/landing-page.js apps/remix-ide/src/app/ui/landing-page/remix-220-home-parity-features.js apps/remix-ide/test/remix-220-home-parity-test.js apps/remix-ide/test/index.js --config apps/remix-ide/.eslintrc --no-ignore
node apps/remix-ide/test/index.js
pnpm nx build remix-ide
node node_modules/typescript/bin/tsc -p apps/remix-ide/tsconfig.app.json --noEmit --skipLibCheck
git diff --check
```

Clean checkout reproduction:

```bash
pnpm install --frozen-lockfile --ignore-scripts
```

A fully fresh checkout that runs normal `pnpm install` also needs network access for the documented postinstall compiler download.

Current unsupported browser coverage: this environment provides Chromium-based browser smoke only. A non-Chromium run cannot be executed from the available Hermes browser tool here, so the non-Chromium part of that checklist item remains an environment limitation unless a Firefox/Safari-capable runner is provided.
